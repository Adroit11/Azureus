{% if error %}
    <div>{{ error.messageKey|trans(error.messageData) }}</div>
{% endif %}

<div class="form-group">
    <form class="login" action="{{ path('login_check') }}" method="post">
        <input type="text" id="username" name="_username" class="form-control" placeholder="Login">
        <input type="password"  id="password" name="_password"  class="form-control" placeholder="Hasło">
        <button type="submit" class="btn btn-default">Zaloguj <span class="glyphicon glyphicon-play" aria-hidden="true"></button>
    </form>
</div>